/**
 * Fixed bottom block height across all 4 screens.
 * Next/Continue, "Already have an account? Sign In", and the home indicator stay in the same position.
 */
export const BOTTOM_BAR_CONTENT_HEIGHT = 200;

export const TEAL = '#5A5CFF';

/** Fixed back button position on every screen that has one. */
export const BACK_BUTTON_TOP = 73;
export const BACK_BUTTON_LEFT = 27;
/** Height reserved for the back button area (top content offset). */
export const BACK_BUTTON_AREA_HEIGHT = BACK_BUTTON_TOP + 56;
